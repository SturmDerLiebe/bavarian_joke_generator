{
  "name": "bavarian_joke_generator",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "jest": "NODE_OPTIONS=--experimental-vm-modules DOTENV_CONFIG_PATH=environment/.env.test jest --setupFiles=dotenv/config --detectOpenHandles",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "@playwright/test": "^1.38.0",
    "@types/node": "^20.6.3",
    "jest": "^29.7.0",
    "prettier": "^3.0.3",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mysql2": "^3.6.1",
    "pug": "^3.0.2"
  }
}
