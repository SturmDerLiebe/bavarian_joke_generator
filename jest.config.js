const config = {
  projects: [
    {
      displayName: "database",
      roots: ["<rootDir>/helpers/database/"],
    },
    {
      displayName: "api",
      roots: ["<rootDir>/helpers/api/"],
    },
  ],
};

export default config;
